import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'android.debug2.app2',
  appName: 'SecondIonic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
